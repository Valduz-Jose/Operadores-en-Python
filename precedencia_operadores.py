# la precedencia de operadores determina el orden en el que se evaluan las expresiones en una operación matemática o lógica. En Python, la precedencia de operadores sigue un conjunto de reglas que dictan el orden en el que se evalúan los operadores en una expresión.
# A continuación se muestra una lista de los operadores en Python, ordenados por precedencia (de mayor a menor):
# Parentesis () tiene la mayor precedencia
# exponente **
# unario +,-
# multiplicación *, división /, módulo %, división entera //
# suma +, resta -
# operadores de comparación ==, !=, >, <, >=, <=
# operadores lógicos and, or, not
# Asignación =, +=, -=, *=, /=, %=, //=, **=

# Ejemplo
resultado = 12 / 3 +2 * 3 -1
print(resultado)